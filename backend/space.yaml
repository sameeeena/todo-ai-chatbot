type: docker
secrets:
  - key: DATABASE_URL
    name: DATABASE_URL
    description: Database connection string (PostgreSQL/Neon/SQLite)
    required: true
  - key: BETTER_AUTH_SECRET
    name: BETTER_AUTH_SECRET
    description: Authentication secret key
    required: true